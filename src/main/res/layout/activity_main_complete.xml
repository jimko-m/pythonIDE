<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <!-- Main Content -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/md_theme_dark_background"
        tools:context=".ui.main.MainActivity">

        <!-- App Bar -->
        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/ThemeOverlay.Material3.Dark">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/md_theme_dark_surface"
                app:title="@string/app_name"
                app:titleTextColor="@color/md_theme_dark_onSurface"
                app:navigationIcon="@drawable/ic_menu_dark"
                app:navigationIconTint="@color/md_theme_dark_onSurface"
                app:menu="@menu/toolbar_menu"
                style="@style/Widget.DarkUI.Toolbar" />

        </com.google.android.material.appbar.AppBarLayout>

        <!-- Fragment Container -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:defaultNavHost="true"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            app:navGraph="@navigation/navigation_complete" />

        <!-- Bottom Navigation -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_nav"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="@color/nav_background_dark"
            app:itemIconTint="@drawable/bottom_nav_color_selector_dark"
            app:itemTextColor="@drawable/bottom_nav_color_selector_dark"
            app:menu="@menu/bottom_nav_menu_complete"
            style="@style/Widget.DarkUI.BottomNavigationView" />

        <!-- Floating Action Button -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/fab_margin"
            android:src="@drawable/ic_add_dark"
            app:layout_anchorGravity="bottom|end"
            app:layout_anchor="@id/bottom_nav"
            app:backgroundTint="@color/md_theme_dark_primary"
            app:tint="@color/md_theme_dark_onPrimary"
            style="@style/Widget.DarkUI.FloatingActionButton" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- Navigation Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:background="@color/drawer_background_dark"
        app:itemIconTint="@drawable/nav_view_color_selector_dark"
        app:itemTextColor="@drawable/nav_view_color_selector_dark"
        app:headerLayout="@layout/nav_header_complete"
        app:menu="@menu/drawer_menu_complete"
        style="@style/Widget.DarkUI.NavigationView" />

</androidx.drawerlayout.widget.DrawerLayout>